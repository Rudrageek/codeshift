// CodeShift warning: a type inferred with medium confidence
// CodeShift warning: b type inferred with medium confidence
// CodeShift warning: return type inferred with medium confidence
function add(a: number, b: number): number {
    return a + b;
}

function multiply(a: number, b: number): number {
    return a * b;
}

function isAdult(age: number): boolean {
    return age >= 18;
}

function greet(name: string): string {
    return "Hello " + name;
}